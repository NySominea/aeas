---
export interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props;
---

<h2
  class:list={[
    "text-2xl font-bold uppercase leading-tight lg:text-3xl",
    className,
  ]}
  set:html={title}
/>
