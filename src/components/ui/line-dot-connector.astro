---
export interface Props {
  lines?: number;
  class?: string;
}

const { lines = 10, class: className } = Astro.props;
---

<div class:list={["flex items-center", className]}>
  <span class="text-zinc-300">{"-".repeat(lines)}</span>
  <span class="h-5 w-5 rounded-full bg-primary"></span>
</div>
